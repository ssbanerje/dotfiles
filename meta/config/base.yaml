- defaults:
    link:
      force: true
      relink: true

- clean:
    ~/:
      force: true
    ~/.local/bin:
      force: true
    ~/.config:
      recursive: true

- ifubuntu:
    - aptupdate:

- ifarch:
    - pacmanupdate:

- ifmacos:
    - bootstraphomebrew:
