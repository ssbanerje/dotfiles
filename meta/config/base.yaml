- defaults:
    link:
      force: true
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- ifubuntu:
    - aptupdate:

- ifarch:
    - pacmanupdate:

- ifmacos:
    - bootstraphomebrew:
